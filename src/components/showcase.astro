---
import ContentSection from "~/components/content-section.astro";
import ShowcaseCard from "~/components/showcase-card.astro";
import { getShowcase } from "~/data/showcase";

const sites = await getShowcase();
---

<ContentSection title="Facilitators" id="facilitators">
  <Fragment slot="lead">
    Three facilitators with <span class="text-primary">superpowers</span>.{" "}
    Join and <span class="text-primary">explore</span> what's possible and get{" "}
    <span class="text-primary">inspired</span>.
  </Fragment>
  <div class="max-w-6xl space-y-2">
    IMAGES AND BIOS
  </div>
</ContentSection>
